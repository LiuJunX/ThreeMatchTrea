@using Match3.Editor.ViewModels
@using Match3.Editor.Logic
@using Match3.Web.Components.Pages
@inject LevelEditorViewModel VM

<aside class="bg-light border-end d-flex flex-column" style="width: 320px; min-width: 320px; overflow-y: auto;">
    <!-- Tab Navigation -->
    <ul class="nav nav-tabs nav-fill border-bottom-0 px-2 pt-2" style="background: #e9ecef;">
        <li class="nav-item">
            <button class="nav-link @(VM.ActiveTabIndex == 0 ? "active" : "")" @onclick="() => VM.ActiveTabIndex = 0">
                <i class="bi bi-grid-3x3"></i> Grid
            </button>
        </li>
        <li class="nav-item">
            <button class="nav-link @(VM.ActiveTabIndex == 1 ? "active" : "")" @onclick="() => VM.ActiveTabIndex = 1">
                <i class="bi bi-folder"></i> Files
            </button>
        </li>
        @if (VM.CurrentMode == EditorMode.Scenario)
        {
            <li class="nav-item">
                <button class="nav-link @(VM.ActiveTabIndex == 2 ? "active" : "")" @onclick="() => VM.ActiveTabIndex = 2">
                    <i class="bi bi-check2-square"></i> Test
                </button>
            </li>
        }
    </ul>

    <div class="flex-grow-1 d-flex flex-column overflow-hidden">
        @if (VM.ActiveTabIndex == 0)
        {
            <!-- Tab: Grid -->
            <div class="p-3 flex-grow-1 overflow-auto">
                <h6 class="text-uppercase text-muted small fw-bold mb-2">Grid Settings</h6>
                <div class="d-flex gap-2 align-items-center mb-3" style="font-size: 0.75rem;">
                    <span class="text-muted">W</span>
                    <input type="number" class="form-control form-control-sm py-0 text-center" style="font-size: 0.75rem; width: 50px;" @bind="VM.EditorWidth" min="1" max="12" />
                    <span class="text-muted">H</span>
                    <input type="number" class="form-control form-control-sm py-0 text-center" style="font-size: 0.75rem; width: 50px;" @bind="VM.EditorHeight" min="1" max="12" />
                    <button class="btn btn-sm btn-secondary py-0 px-2" style="font-size: 0.75rem;" @onclick="VM.ResizeGrid">Apply</button>
                </div>

                @if (VM.CurrentMode == EditorMode.Level)
                {
                    <div class="d-flex gap-2 align-items-center mb-3" style="font-size: 0.75rem;">
                        <span class="text-muted">Moves</span>
                        <input type="number" class="form-control form-control-sm py-0 text-center" style="font-size: 0.75rem; width: 60px;" @bind="VM.CurrentLevel.MoveLimit" />
                    </div>

                    <!-- Objectives Editor (Compact) -->
                    <div class="mb-3 p-2 border rounded bg-white">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <h6 class="text-uppercase text-muted small fw-bold mb-0">
                                <i class="bi bi-bullseye me-1"></i>
                                Objectives
                            </h6>
                            @if (VM.ActiveObjectiveCount < LevelEditorViewModel.MaxObjectives)
                            {
                                <button class="btn btn-sm btn-outline-primary p-0 px-2" @onclick="VM.AddObjective" title="Add Objective" style="font-size: 0.85rem; line-height: 1;">+</button>
                            }
                        </div>

                        @for (int i = 0; i < VM.Objectives.Length; i++)
                        {
                            var obj = VM.Objectives[i];
                            var index = i;
                            if (obj.TargetLayer != Match3.Core.Models.Enums.ObjectiveTargetLayer.None)
                            {
                                <div class="d-flex gap-1 align-items-center mb-1" style="font-size: 0.75rem;">
                                    <select class="form-select form-select-sm py-0" style="font-size: 0.75rem; width: 70px;"
                                            value="@obj.TargetLayer"
                                            @onchange="e => OnObjectiveLayerChanged(index, e)">
                                        @foreach (var layer in LevelEditorViewModel.ObjectiveTargetLayers)
                                        {
                                            if (layer != Match3.Core.Models.Enums.ObjectiveTargetLayer.None)
                                            {
                                                <option value="@layer">@layer</option>
                                            }
                                        }
                                    </select>
                                    <select class="form-select form-select-sm py-0 flex-grow-1" style="font-size: 0.75rem;"
                                            value="@obj.ElementType"
                                            @onchange="e => OnObjectiveElementChanged(index, e)">
                                        @foreach (var (value, name) in LevelEditorViewModel.GetElementTypesForLayer(obj.TargetLayer))
                                        {
                                            <option value="@value">@name</option>
                                        }
                                    </select>
                                    <input type="number" class="form-control form-control-sm py-0 text-center" style="font-size: 0.75rem; width: 50px;"
                                           value="@obj.TargetCount"
                                           min="1"
                                           @onchange="e => OnObjectiveCountChanged(index, e)" />
                                    <button class="btn btn-sm p-0 text-danger" style="font-size: 0.7rem; line-height: 1;" @onclick="() => VM.RemoveObjective(index)" title="Remove">✕</button>
                                </div>
                            }
                        }

                        @if (VM.ActiveObjectiveCount == 0)
                        {
                            <div class="text-muted small text-center py-1" style="font-size: 0.75rem;">
                                No objectives
                            </div>
                        }
                    </div>
                }

            </div>
        }
        else if (VM.ActiveTabIndex == 1)
        {
            @if (VM.CurrentMode == EditorMode.Level)
            {
                <!-- Tab: Files (Level Mode - Level File Browser) -->
                <div class="p-3 flex-grow-1 overflow-auto d-flex flex-column">
                    <!-- Save Actions -->
                    <div class="mb-3">
                        <div class="d-flex gap-2">
                            @if (!string.IsNullOrEmpty(VM.CurrentLevelFilePath))
                            {
                                <button class="btn btn-sm btn-primary flex-grow-1" @onclick="OnSaveLevel">
                                    <i class="bi bi-save me-1"></i> Save
                                </button>
                            }
                            <button class="btn btn-sm btn-outline-primary flex-grow-1" @onclick="OnSaveAsNewLevel">
                                <i class="bi bi-file-earmark-plus me-1"></i> Save As New
                            </button>
                        </div>
                        @if (!string.IsNullOrEmpty(VM.CurrentLevelFilePath))
                        {
                            <div class="small text-muted mt-1 text-truncate" title="@VM.CurrentLevelFilePath">
                                <i class="bi bi-file-earmark me-1"></i>@System.IO.Path.GetFileName(VM.CurrentLevelFilePath)
                            </div>
                        }
                    </div>

                    <div class="flex-grow-1 d-flex flex-column min-h-0">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <h6 class="text-uppercase text-muted small fw-bold mb-0">Level Files</h6>
                            <button class="btn btn-sm btn-link p-0 text-decoration-none" @onclick="VM.RefreshLevelList">
                                <i class="bi bi-arrow-clockwise"></i>
                            </button>
                        </div>

                        <div class="border rounded bg-white flex-grow-1 overflow-auto">
                            @if (VM.RootLevelFolderNode != null)
                            {
                                <ScenarioTreeItem Folder="VM.RootLevelFolderNode"
                                                  CurrentFilePath="@VM.CurrentLevelFilePath"
                                                  ExpandedPaths="VM.LevelExpandedPaths"
                                                  OnSelectFile="OnSelectLevelFile"
                                                  OnOpenFile="OnOpenLevelFile"
                                                  OnContextMenuAction="OnLevelContextMenuAction" />
                            }
                            else
                            {
                                <div class="p-3 text-center text-muted small">Loading...</div>
                            }
                        </div>
                    </div>
                </div>
            }
            else
            {
                <!-- Tab: Files (Scenario Mode - Scenario Info + File Browser) -->
                <div class="p-3 flex-grow-1 overflow-auto d-flex flex-column">
                    <div class="mb-3">
                        <h6 class="text-uppercase text-muted small fw-bold mb-2">Scenario Info</h6>
                        <label class="form-label small mb-1">Name</label>
                        <input type="text" class="form-control form-control-sm mb-2" @bind="VM.ScenarioName" @bind:event="oninput" />

                        <label class="form-label small mb-1">Description</label>
                        <textarea class="form-control form-control-sm" rows="2" @bind="VM.ScenarioDescription" @bind:event="oninput"></textarea>
                    </div>

                    <div class="flex-grow-1 d-flex flex-column min-h-0">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <h6 class="text-uppercase text-muted small fw-bold mb-0">Files</h6>
                            <button class="btn btn-sm btn-link p-0 text-decoration-none" @onclick="VM.RefreshScenarioList">
                                <i class="bi bi-arrow-clockwise"></i>
                            </button>
                        </div>

                        <div class="border rounded bg-white flex-grow-1 overflow-auto">
                            @if (VM.RootFolderNode != null)
                            {
                                <ScenarioTreeItem Folder="VM.RootFolderNode"
                                                  CurrentFilePath="@VM.CurrentFilePath"
                                                  ExpandedPaths="VM.ExpandedPaths"
                                                  OnSelectFile="OnSelectScenarioFile"
                                                  OnOpenFile="OnOpenScenarioFile"
                                                  OnContextMenuAction="OnContextMenuAction" />
                            }
                            else
                            {
                                <div class="p-3 text-center text-muted small">Loading...</div>
                            }
                        </div>
                    </div>
                </div>
            }
        }
        else if (VM.ActiveTabIndex == 2)
        {
            <!-- Tab: Test (Assertions) -->
            <div class="p-3 flex-grow-1 overflow-auto">
                <h6 class="text-uppercase text-muted small fw-bold mb-3">Assertions</h6>

                @if (!VM.IsAssertionMode)
                {
                    <p class="small text-muted mb-3">
                        Define expected tile states after test execution. Click "Edit" to start placing assertions on the grid.
                    </p>
                    <button class="btn btn-warning btn-sm w-100" @onclick="VM.ToggleAssertionMode">
                        <i class="bi bi-pencil"></i> Edit Assertions
                    </button>
                }
                else
                {
                    <div class="bg-warning bg-opacity-10 p-3 rounded border border-warning mb-3">
                        <div class="small fw-bold text-warning-emphasis mb-2">Editing Mode</div>

                        <div class="form-check form-switch small mb-1">
                            <input class="form-check-input" type="checkbox" id="assertColor" @bind="VM.AssertColor">
                            <label class="form-check-label" for="assertColor">Match Color</label>
                        </div>
                        <div class="form-check form-switch small mb-3">
                            <input class="form-check-input" type="checkbox" id="assertBomb" @bind="VM.AssertBomb">
                            <label class="form-check-label" for="assertBomb">Match Bomb</label>
                        </div>

                        <div class="small text-muted mb-3" style="font-size: 0.75rem;">
                            Select palette items, then click grid cells to set expectations.
                        </div>

                        <button class="btn btn-success btn-sm w-100" @onclick="VM.ToggleAssertionMode">
                            <i class="bi bi-check-lg"></i> Done
                        </button>
                    </div>
                }

                <div class="mt-3">
                    <div class="small text-muted mb-2">
                        Assertions: @VM.CurrentScenario.Assertions.Count
                    </div>
                    @if (VM.CurrentScenario.Assertions.Count > 0)
                    {
                        <div class="border rounded bg-white p-2" style="max-height: 200px; overflow-y: auto;">
                            <ul class="list-unstyled small mb-0 font-monospace">
                                @foreach (var a in VM.CurrentScenario.Assertions)
                                {
                                    <li class="d-flex justify-content-between align-items-center py-1 border-bottom">
                                        <span>(@a.X, @a.Y)</span>
                                        <span class="text-muted">
                                            @(a.Type?.ToString() ?? "*")
                                            @(a.Bomb.HasValue ? GetBombIcon(a.Bomb.Value) : "")
                                        </span>
                                    </li>
                                }
                            </ul>
                        </div>
                    }
                </div>
            </div>
        }
    </div>

    <!-- Context Menu (for file browser) -->
    @if (ShowContextMenu)
    {
        <div class="position-fixed top-0 start-0 w-100 h-100" style="z-index: 2000; background: rgba(0,0,0,0.1);"
             @onclick="CloseContextMenu" @oncontextmenu="CloseContextMenu" @oncontextmenu:preventDefault="true"></div>
        <div class="position-fixed bg-white shadow-lg rounded border p-2"
             style="z-index: 2001; left: @(ContextMenuX)px; top: @(ContextMenuY)px; min-width: 150px;">
            <h6 class="dropdown-header small text-truncate">@ContextMenuTargetName</h6>
            @if (ContextMenuType == "folder")
            {
                @if (IsLevelContextMenu)
                {
                    <button class="btn btn-sm btn-light w-100 text-start mb-1" @onclick="CreateNewLevel">
                        <i class="bi bi-file-earmark-plus me-2"></i> New Level
                    </button>
                }
                else
                {
                    <button class="btn btn-sm btn-light w-100 text-start mb-1" @onclick="CreateNewScenario">
                        <i class="bi bi-file-earmark-plus me-2"></i> New Scenario
                    </button>
                }
                <button class="btn btn-sm btn-light w-100 text-start" @onclick="CreateNewFolder">
                    <i class="bi bi-folder-plus me-2"></i> New Folder
                </button>
            }
            else
            {
                @if (IsLevelContextMenu)
                {
                    <button class="btn btn-sm btn-light w-100 text-start mb-1" @onclick="DuplicateLevel">
                        <i class="bi bi-files me-2"></i> Duplicate
                    </button>
                }
                else
                {
                    <button class="btn btn-sm btn-light w-100 text-start mb-1" @onclick="DuplicateScenario">
                        <i class="bi bi-files me-2"></i> Duplicate
                    </button>
                }
                <hr class="dropdown-divider my-1">
                <button class="btn btn-sm btn-danger w-100 text-start" @onclick="DeleteFile">
                    <i class="bi bi-trash me-2"></i> Delete
                </button>
            }
        </div>
    }
</aside>

@implements IDisposable

@code {
    protected override void OnInitialized()
    {
        VM.PropertyChanged += OnVmPropertyChanged;
    }

    private void OnVmPropertyChanged(object? sender, System.ComponentModel.PropertyChangedEventArgs e)
    {
        InvokeAsync(StateHasChanged);
    }

    public void Dispose()
    {
        VM.PropertyChanged -= OnVmPropertyChanged;
    }

    // Context menu state (kept in View for now - UI-only state)
    private bool ShowContextMenu = false;
    private double ContextMenuX = 0;
    private double ContextMenuY = 0;
    private string ContextMenuType = "";
    private string ContextMenuPath = "";
    private bool IsLevelContextMenu = false;
    private string ContextMenuTargetName => System.IO.Path.GetFileName(ContextMenuPath);

    // --- Scenario File Handlers ---
    private void OnSelectScenarioFile(string path)
    {
        VM.CurrentFilePath = path;
    }

    private async Task OnOpenScenarioFile(string path)
    {
        await VM.LoadScenarioAsync(path);
    }

    private void OnContextMenuAction((MouseEventArgs e, string type, string path) args)
    {
        IsLevelContextMenu = false;
        ContextMenuType = args.type;
        ContextMenuPath = args.path;
        SetContextMenuPosition(args.e);
        ShowContextMenu = true;
    }

    // --- Level File Handlers ---
    private void OnSelectLevelFile(string path)
    {
        VM.CurrentLevelFilePath = path;
    }

    private async Task OnOpenLevelFile(string path)
    {
        await VM.LoadLevelAsync(path);
    }

    private void OnLevelContextMenuAction((MouseEventArgs e, string type, string path) args)
    {
        IsLevelContextMenu = true;
        ContextMenuType = args.type;
        ContextMenuPath = args.path;
        SetContextMenuPosition(args.e);
        ShowContextMenu = true;
    }

    // --- Context Menu Helpers ---
    private void SetContextMenuPosition(MouseEventArgs? e)
    {
        if (e != null)
        {
            ContextMenuX = e.ClientX;
            ContextMenuY = e.ClientY;
        }
        else
        {
            ContextMenuX = 100;
            ContextMenuY = 100;
        }

        if (ContextMenuX > 1000) ContextMenuX = 800;
    }

    private void CloseContextMenu()
    {
        ShowContextMenu = false;
    }

    private async Task CreateNewScenario()
    {
        CloseContextMenu();
        await VM.CreateNewScenarioAsync(ContextMenuPath);
    }

    private async Task CreateNewLevel()
    {
        CloseContextMenu();
        await VM.CreateNewLevelAsync(ContextMenuPath);
    }

    private async Task CreateNewFolder()
    {
        CloseContextMenu();
        if (IsLevelContextMenu)
        {
            await VM.CreateNewLevelFolderAsync(ContextMenuPath);
        }
        else
        {
            await VM.CreateNewFolderAsync(ContextMenuPath);
        }
    }

    private async Task DuplicateScenario()
    {
        CloseContextMenu();
        await VM.DuplicateScenarioAsync(ContextMenuPath);
    }

    private async Task DuplicateLevel()
    {
        CloseContextMenu();
        await VM.DuplicateLevelAsync(ContextMenuPath);
    }

    private async Task OnSaveLevel()
    {
        await VM.SaveLevelAsync();
    }

    private async Task OnSaveAsNewLevel()
    {
        await VM.CreateNewLevelAsync("");
    }

    private async Task DeleteFile()
    {
        CloseContextMenu();
        if (IsLevelContextMenu)
        {
            await VM.DeleteLevelFileAsync(ContextMenuPath, ContextMenuType == "folder");
        }
        else
        {
            await VM.DeleteFileAsync(ContextMenuPath, ContextMenuType == "folder");
        }
    }

    private string GetBombIcon(Match3.Core.Models.Enums.BombType bomb) => bomb switch
    {
        Match3.Core.Models.Enums.BombType.Horizontal => "↔",
        Match3.Core.Models.Enums.BombType.Vertical => "↕",
        Match3.Core.Models.Enums.BombType.Ufo => "U",
        Match3.Core.Models.Enums.BombType.Square5x5 => "S",
        Match3.Core.Models.Enums.BombType.Color => "C",
        _ => ""
    };

    // --- Objective Editing Handlers ---
    private void OnObjectiveLayerChanged(int index, ChangeEventArgs e)
    {
        if (Enum.TryParse<Match3.Core.Models.Enums.ObjectiveTargetLayer>(e.Value?.ToString(), out var layer))
        {
            VM.SetObjectiveLayer(index, layer);
        }
    }

    private void OnObjectiveElementChanged(int index, ChangeEventArgs e)
    {
        if (int.TryParse(e.Value?.ToString(), out var elementType))
        {
            VM.SetObjectiveElementType(index, elementType);
        }
    }

    private void OnObjectiveCountChanged(int index, ChangeEventArgs e)
    {
        if (int.TryParse(e.Value?.ToString(), out var count))
        {
            VM.SetObjectiveTargetCount(index, count);
        }
    }
}
